package Principles.SRP;

// Handles generating reports for employees
public class EmployeeReportGenerator {
    public void generatePdfReport(Employee employee) {
        // code to generate PDF report for the given employee
        System.out.println("Generating PDF report for " + employee.getName());
    }
}
